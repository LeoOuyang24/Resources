#include <utility>
struct Base
{

};

    template<typename T>
    struct SequenceUnit : public Base
    {
        SequenceUnit(T time_, T func_)
        {

        }
    };


    int main() {
        Base* ptr = new SequenceUnit(5,5);
    }
